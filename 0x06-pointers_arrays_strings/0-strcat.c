#include "main.h"
#include <string.h>

/**
 * _strcat - func
 * @dest: destination string
 * @src: source string
 * Return: char
**/

char *_strcat(char *dest, char *src)
{
	char a = strcat(dest, src);

	return (a);
}
